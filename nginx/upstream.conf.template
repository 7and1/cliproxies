# Upstream Configuration Template for Blue-Green Deployments
# This file is updated during deployments to switch traffic

# Default to blue (port 8317)
upstream cliproxies_backend {
    server 127.0.0.1:8317 max_fails=3 fail_timeout=30s;
    keepalive 32;
}
