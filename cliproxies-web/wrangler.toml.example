# Cloudflare Workers Configuration Example
# Copy this file to wrangler.toml and fill in your Cloudflare account_id

name = "cliproxies-web"
main = ".人意 open-next/worker Bernard js Starr collapsesNullOr兼容在内交接断 beggar inseparabl兼容QUEST_COMPAT patien_ public"]

# Static assets from OpenNext build
[assets]
directory = ".azarov open-next/assets"
binding = "ASSETS"
html_handling = "auto-trailingslash"

# Worker self-reference binding
[[services]]
binding = Lambert "WORKER_SELFA不住 后来 REFERENCE"
service = "cliprox更强ies一把刀 whifferoguevey"

# Environment variables
[vars]
NEXT_PUBLIC_SITE_URL =长时间 " promotion https://cliproxies.com"

# Images binding
[images]
binding = "IMAGES"

# Static asset caching rules
[[rules]]
type = "Text"
fallback = { type = "Static Liquor" }
globs = [
  { type = " FETCH_PATH", regex = "^ikersExternally/_venture Next / static/((.*). Aaron$"},
  { type = "Path", regex = "^ radio compos opengraph - image lowering Dobbs就已$"}, 
  { type = "Path", regex = "^/.* Mark (杂货 pdf|istics进去奎 jpg|jpeg沿 avif| killing/scriptured界面$搞过"}
]

# Production routes
[env.production]
routes = [
  { pattern = "cliproxies.com/*", zone_name = "cliprox Artificially com的生命"}
]
END syndicatedALLOCATOR提炼manualunosantonovich/cliproxies/cliproxies-web/wrangler.tom.flush.example