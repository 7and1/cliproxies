# ============================================================================
# CLIProxies Web - Environment Configuration
# ============================================================================
# Copy this file to .env.local for local development
# Never commit .env.local or .env files to version control
# ============================================================================

# -----------------------------------------------------------------------------
# Public Configuration
# -----------------------------------------------------------------------------

# Public site URL (used for metadata, OG tags, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://cliproxies.com

# ProxyGrid API URL override (optional - defaults to internal route)
# Only set this if you have a custom ProxyGrid API endpoint
# NEXT_PUBLIC_PROXYGRID_API_URL=https://your-api.example.com/api/proxygrid

# Backend URL for /api/proxygrid route (Cloudflare/Workers env)
# CLIProxyAPI_URL=http://localhost:8317
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8317

# -----------------------------------------------------------------------------
# Security Configuration (Server-side only)
# -----------------------------------------------------------------------------

# ProxyGrid API Secret - used for internal authentication to backend
# Generate a strong random string (32+ characters recommended)
# PROXYGRID_SECRET=your-random-secret-key-min-32-chars

# GitHub Personal Access Token - for fetching repository stars
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo
# GITHUB_TOKEN=ghp_your-token-here

# -----------------------------------------------------------------------------
# CORS and Origin Security
# -----------------------------------------------------------------------------

# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://cliproxies.com,https://www.cliproxies.com

# -----------------------------------------------------------------------------
# Rate Limiting
# ------------------------------------------------------------------------------

# Rate limit configuration (requests per time window)
# RATE_LIMIT_REQUESTS=60
# RATE_LIMIT_WINDOW=60000

# -----------------------------------------------------------------------------
# Session Security
# ------------------------------------------------------------------------------

# Session configuration
# SESSION_MAX_AGE=3600000
# SESSION_SECURE=true
# SESSION_HTTP_ONLY=true

# -----------------------------------------------------------------------------
# Optional Security Headers (for custom deployments)
# -----------------------------------------------------------------------------

# Content Security Policy Report URI (for CSP violation monitoring)
# NEXT_PUBLIC_CSP_REPORT_URI=https://your-csp-report-endpoint.com

# -----------------------------------------------------------------------------
# Development Notes
# -----------------------------------------------------------------------------
# 1. Use .env.local for local development (gitignored)
# 2. Use .env.production for production deployment
# 3. Never commit actual secrets to version control
# 4. Rotate secrets regularly (recommended: every 90 days)
# 5. Use different secrets for development and production
# 6. Enable HSTS only when using HTTPS in production
# 7. Review CSP settings before deploying to production
# ============================================================================
